1. Initial problem statement:
The goal is to create and analyze a database of movie reviews submitted by the public. For this I will use the open database, provided by GroupLens.

Points 2-6 are accomplished using the "create_db.py" script and associated .sql files

The python script downloads the raw data to a "data_cache" file.

7. 3 data quality risks:
- potential errors with missing/invalid movieId values
- use of free text in reviews might cause problems
- movie titles in the dataset were entered manually and might be inconsistent or contain errors

ERD and HLD graphs are included as "BGD_2-ERD.png" and "BGD_2-HLD.png"



